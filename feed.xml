<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.2">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml"/><link href="/" rel="alternate" type="text/html" hreflang="en"/><updated>2023-08-04T22:03:33+00:00</updated><id>/feed.xml</id><title type="html">Analytical Methods for Global Policy Studies (PA397G)</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Week 0 - Prepare for new semester</title><link href="/blog/2023/w0/" rel="alternate" type="text/html" title="Week 0 - Prepare for new semester"/><published>2023-07-29T00:00:00+00:00</published><updated>2023-07-29T00:00:00+00:00</updated><id>/blog/2023/w0</id><content type="html" xml:base="/blog/2023/w0/"><![CDATA[<h3 id="the-greek-alphabet">The Greek Alphabet</h3> <hr/> <p>English pronunciation for math &amp; science</p> <iframe width="560" height="315" src="https://www.youtube-nocookie.com/embed/PStgY5AcEIw" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""></iframe> <h3 id="common-math-notations-with-python-examples">Common math notations with Python examples</h3> <hr/> <p><em>Adapted from <a href="https://github.com/Jam3/math-as-code">math-as-code</a>. Programming is just for your reference, not a required component.</em></p> <p>Mathematical symbols can mean different things depending on the author, context and the field of study (linear algebra, set theory, etc). This guide may not cover <em>all</em> uses of a symbol. In some cases, real-world references (blog posts, publications, etc) will be cited to demonstrate how a symbol might appear in the wild.</p> <p>For a more complete list, refer to <a href="https://en.wikipedia.org/wiki/List_of_mathematical_symbols">Wikipedia - List of Mathematical Symbols</a>.</p> <p>For simplicity, many of the code examples here operate on floating point values and are not numerically robust. For more details on why this may be a problem, see <a href="https://github.com/mikolalysenko/robust-arithmetic-notes">Robust Arithmetic Notes</a> by Mikola Lysenko.</p> <ul> <li><a href="#variable-name-conventions">variable name conventions</a></li> <li><a href="#equals-symbols">equals <code class="language-plaintext highlighter-rouge">=</code> <code class="language-plaintext highlighter-rouge">≈</code> <code class="language-plaintext highlighter-rouge">≠</code> <code class="language-plaintext highlighter-rouge">:=</code></a></li> <li><a href="#square-root-and-complex-numbers">square root and complex numbers <code class="language-plaintext highlighter-rouge">√</code> <em><code class="language-plaintext highlighter-rouge">i</code></em></a></li> <li><a href="#dot--cross">dot &amp; cross <code class="language-plaintext highlighter-rouge">·</code> <code class="language-plaintext highlighter-rouge">×</code> <code class="language-plaintext highlighter-rouge">∘</code></a> <ul> <li><a href="#scalar-multiplication">scalar multiplication</a></li> <li><a href="#vector-multiplication">vector multiplication</a></li> <li><a href="#dot-product">dot product</a></li> <li><a href="#cross-product">cross product</a></li> </ul> </li> <li><a href="#sigma">sigma <code class="language-plaintext highlighter-rouge">Σ</code></a> - <em>summation</em></li> <li><a href="#capital-pi">capital Pi <code class="language-plaintext highlighter-rouge">Π</code></a> - <em>products of sequences</em></li> <li><a href="#pipes">pipes <code class="language-plaintext highlighter-rouge">||</code></a> <ul> <li><a href="#absolute-value">absolute value</a></li> <li><a href="#euclidean-norm">Euclidean norm</a></li> <li><a href="#determinant">determinant</a></li> </ul> </li> <li><a href="#hat">hat <strong><code class="language-plaintext highlighter-rouge">â</code></strong></a> - <em>unit vector</em></li> <li><a href="#element">“element of” <code class="language-plaintext highlighter-rouge">∈</code> <code class="language-plaintext highlighter-rouge">∉</code></a></li> <li><a href="#common-number-sets">common number sets <code class="language-plaintext highlighter-rouge">ℝ</code> <code class="language-plaintext highlighter-rouge">ℤ</code> <code class="language-plaintext highlighter-rouge">ℚ</code> <code class="language-plaintext highlighter-rouge">ℕ</code></a></li> <li><a href="#function">function <code class="language-plaintext highlighter-rouge">ƒ</code></a> <ul> <li><a href="#piecewise-function">piecewise function</a></li> <li><a href="#common-functions">common functions</a></li> <li><a href="#function-notation">function notation <code class="language-plaintext highlighter-rouge">↦</code> <code class="language-plaintext highlighter-rouge">→</code></a></li> </ul> </li> <li><a href="#prime">prime <code class="language-plaintext highlighter-rouge">′</code></a></li> <li><a href="#floor--ceiling">floor &amp; ceiling <code class="language-plaintext highlighter-rouge">⌊</code> <code class="language-plaintext highlighter-rouge">⌉</code></a></li> <li><a href="#arrows">arrows</a> <ul> <li><a href="#material-implication">material implication <code class="language-plaintext highlighter-rouge">⇒</code> <code class="language-plaintext highlighter-rouge">→</code></a></li> <li><a href="#equality">equality <code class="language-plaintext highlighter-rouge">&lt;</code> <code class="language-plaintext highlighter-rouge">≥</code> <code class="language-plaintext highlighter-rouge">≫</code></a></li> <li><a href="#conjunction--disjunction">conjunction &amp; disjunction <code class="language-plaintext highlighter-rouge">∧</code> <code class="language-plaintext highlighter-rouge">∨</code></a></li> </ul> </li> <li><a href="#logical-negation">logical negation <code class="language-plaintext highlighter-rouge">¬</code> <code class="language-plaintext highlighter-rouge">~</code> <code class="language-plaintext highlighter-rouge">!</code></a></li> <li><a href="#intervals">intervals</a></li> <li><a href="#more">more…</a></li> </ul> <h4 id="variable-name-conventions">variable name conventions</h4> <p>There are a variety of naming conventions depending on the context and field of study, and they are not always consistent. However, in some of the literature you may find variable names to follow a pattern like so:</p> <ul> <li><em>s</em> - italic lowercase letters for scalars (e.g. a number)</li> <li><strong>x</strong> - bold lowercase letters for vectors (e.g. a 2D point)</li> <li><strong>A</strong> - bold uppercase letters for matrices (e.g. a 3D transformation)</li> <li><em>θ</em> - italic lowercase Greek letters for constants and special variables (e.g. <a href="https://en.wikipedia.org/wiki/Spherical_coordinate_system">polar angle <em>θ</em>, <em>theta</em></a>)</li> </ul> <p>This will also be the format of this guide.</p> <h5 id="numpy">Numpy</h5> <p>Numpy is a powerful <strong>array programming</strong> library, which in python can be interpreted as a <strong>domain specific language</strong> (DSL). Sometimes it’s helpful to think of math in python as two languages sharing a namespace, with special <a href="https://en.wikipedia.org/wiki/Syntactic_sugar">syntax sugar</a> to access one or the other. This will be important in our vectors and matrices section, because <em>slightly different python syntax means different speeds at large input</em>. The convention is <code class="language-plaintext highlighter-rouge">import numpy as np</code>, so when you see <code class="language-plaintext highlighter-rouge">np.something</code> you know that we’re working in numpy.</p> <h4 id="equals-symbols">equals symbols</h4> <p>There are a number of symbols resembling the equals sign <code class="language-plaintext highlighter-rouge">=</code>. Here are a few common examples:</p> <ul> <li><code class="language-plaintext highlighter-rouge">=</code> is for equality (values are the same)</li> <li><code class="language-plaintext highlighter-rouge">≠</code> is for inequality (value are not the same)</li> <li><code class="language-plaintext highlighter-rouge">≈</code> is for approximately equal to (<code class="language-plaintext highlighter-rouge">π ≈ 3.14159</code>)</li> <li><code class="language-plaintext highlighter-rouge">:=</code> is for definition (A is defined as B)</li> </ul> <p>In Python:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">## equality
</span><span class="mi">2</span> <span class="o">==</span> <span class="mi">3</span>

<span class="c1">## inequality
</span><span class="mi">2</span> <span class="o">!=</span> <span class="mi">3</span>

<span class="c1">## approximately equal
</span><span class="kn">import</span> <span class="n">math</span>
<span class="n">math</span><span class="p">.</span><span class="nf">isclose</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="n">pi</span><span class="p">,</span> <span class="mf">3.14159</span><span class="p">)</span> <span class="c1"># math.isclose doesn't have a third argument for tolerance, so this is false
</span>
<span class="kn">from</span> <span class="n">numpy.testing</span> <span class="kn">import</span> <span class="n">assert_almost_equal</span>
<span class="nf">assert_almost_equal</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="n">pi</span><span class="p">,</span> <span class="mf">3.14159</span><span class="p">,</span> <span class="mf">1e-5</span><span class="p">)</span> <span class="c1"># we gave it a the tolerance we want, 5 decimal places. 
# This is actually a unit test, equivalent to "assert isclose(x,y)", read on for more. 
</span>
<span class="k">def</span> <span class="nf">almost_equal</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">epsilon</span><span class="o">=</span><span class="mi">7</span><span class="p">):</span> 
  <span class="sh">'''</span><span class="s"> you can make your own! 
  in numpy, 1e-7 is the default epsilon
  </span><span class="sh">'''</span>
  <span class="k">return</span> <span class="nf">abs</span><span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="n">y</span><span class="p">)</span> <span class="o">&lt;</span> <span class="mi">10</span> <span class="o">**</span> <span class="o">-</span><span class="n">epsilon</span>

</code></pre></div></div> <blockquote> <p><strong>Read more</strong>: programmers got this idea from the <a href="https://en.wikipedia.org/wiki/(%CE%B5,_%CE%B4)-definition_of_limit">epsilon-delta definition of limit</a></p> </blockquote> <p><strong>Note</strong>: subclasses of <a href="https://docs.python.org/3/library/unittest.html"><code class="language-plaintext highlighter-rouge">unittest.TestCase</code></a> come with their own <code class="language-plaintext highlighter-rouge">assertAlmostEqual</code>.</p> <p><strong>Warning</strong>: <em>please</em> don’t use exact <code class="language-plaintext highlighter-rouge">==</code> equality on floats!</p> <p>In mathematical notation, you might see the <code class="language-plaintext highlighter-rouge">:=</code>, <code class="language-plaintext highlighter-rouge">=:</code> and <code class="language-plaintext highlighter-rouge">=</code> symbols being used for <em>definition</em>.<sup><a href="http://mimosa-pudica.net/improved-oren-nayar.html#images">2</a></sup></p> <p>For example, the following defines <em>x</em> to be another name for 2<em>kj</em>.</p> <p><img src="http://latex.codecogs.com/svg.latex?x%20%3A%3D%202kj" alt="equals1"/></p> <p>In python, we <em>define</em> our variables and provide aliases with <code class="language-plaintext highlighter-rouge">=</code>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="mi">2</span> <span class="o">*</span> <span class="n">k</span> <span class="o">*</span> <span class="n">j</span>
</code></pre></div></div> <p><em>Assignment</em> in python is generally <em>mutable</em> besides special cases like <code class="language-plaintext highlighter-rouge">Tuple</code>.</p> <blockquote> <p><strong>Note</strong>: Some languages have pre-processor <code class="language-plaintext highlighter-rouge">#define</code> statements, which are closer to a mathematical <em>define</em>.</p> </blockquote> <p>Notice that <code class="language-plaintext highlighter-rouge">def</code> is a form of <code class="language-plaintext highlighter-rouge">:=</code> as well.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">plus</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">):</span> 
  <span class="k">return</span> <span class="n">x</span> <span class="o">+</span> <span class="n">y</span>
</code></pre></div></div> <p>The following, on the other hand, represents equality:</p> <p><img src="http://latex.codecogs.com/svg.latex?x%20%3D%202kj" alt="equals2"/></p> <p><strong>Important</strong>: the difference between <code class="language-plaintext highlighter-rouge">=</code> and <code class="language-plaintext highlighter-rouge">==</code> can be more obvious in code than it is in math literature! In python, a <code class="language-plaintext highlighter-rouge">=</code> is an <em>instruction</em>. You’re telling the machine to interact with the namespace, add something to it or change something in it. In python, when you write <code class="language-plaintext highlighter-rouge">==</code> you’re asking the machine “may I have a <code class="language-plaintext highlighter-rouge">bool</code>?”. In math, the former case is <em>either</em> covered by <code class="language-plaintext highlighter-rouge">:=</code> or <code class="language-plaintext highlighter-rouge">=</code>, while the latter case is <em>usually</em> <code class="language-plaintext highlighter-rouge">=</code>, and you might have to do some disambiguating in your reading.</p> <p>In math, when I write 1 + 1 = 2 I’m making a <em>judgment</em>. It’s not that i’m asking the world (or the chalkboard) for a bool, it’s that I’m keeping track of my beliefs. This distinction is the foundation of <em>unit tests</em> or <em>assertions</em>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># assert in python takes an expression that lands in bool and a string to be printed if it turns out false. 
</span><span class="k">assert</span> <span class="nf">plus</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="o">==</span> <span class="mi">2</span><span class="p">,</span> <span class="sh">"</span><span class="s">DANGER: PHYSICS IS BROKEN. PLEASE STAY INSIDE. </span><span class="sh">"</span>
</code></pre></div></div> <p>It’s important to know <em>when a falsehood ought to crash a program</em> vs. when you just want a boolean value. To understand this better, read <a href="https://en.wikipedia.org/wiki/G%C3%B6del,_Escher,_Bach">this</a>.</p> <h4 id="square-root-and-complex-numbers">square root and complex numbers</h4> <p>A square root operation is of the form:</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Cleft%28%5Csqrt%7Bx%7D%5Cright%29%5E2%20%3D%20x" alt="squareroot"/></p> <p>In programming we use a <code class="language-plaintext highlighter-rouge">sqrt</code> function, like so:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">math</span>
<span class="nf">print</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">2</span><span class="p">))</span>
<span class="c1"># Out: 1.4142135623730951
</span>
<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="nf">print</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">2</span><span class="p">))</span>
<span class="c1"># Out: 1.4142135623730951
</span>
</code></pre></div></div> <p>Complex numbers are expressions of the form <img src="http://latex.codecogs.com/svg.latex?a&space;&plus;&space;ib" alt="complex"/>, where <img src="http://latex.codecogs.com/svg.latex?a" alt="a"/> is the real part and <img src="http://latex.codecogs.com/svg.latex?b" alt="b"/> is the imaginary part. The imaginary number <img src="http://latex.codecogs.com/svg.latex?i" alt="i"/> is defined as:</p> <p><img src="http://latex.codecogs.com/svg.latex?i%3D%5Csqrt%7B-1%7D" alt="imaginary"/>. </p> <p>Vanilla python has a <code class="language-plaintext highlighter-rouge">complex</code> constructor, and a standard module <code class="language-plaintext highlighter-rouge">cmath</code> for working with them.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">complex</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">)</span>
<span class="c1"># Out: (1+1j)s
</span>
<span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="nf">complex</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">))</span>
<span class="c1"># TypeError: can't convert complex to float
</span>
<span class="kn">import</span> <span class="n">cmath</span>
<span class="n">cmath</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="nf">complex</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">1</span><span class="p">))</span>
<span class="c1"># Out: (1.09868411346781+0.45508986056222733j)
</span>
<span class="c1"># you need numpy to get conjugate
</span><span class="n">np</span><span class="p">.</span><span class="nf">conj</span><span class="p">(</span><span class="nf">complex</span><span class="p">(</span><span class="mf">0.5</span><span class="p">,</span><span class="mf">0.5</span><span class="p">))</span>
<span class="c1"># Out: (0.5-0.5j)
</span>
<span class="c1"># we can represent the basic meaning of the imaginary unit like so
</span><span class="k">assert</span> <span class="n">cmath</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="nf">complex</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">))</span> <span class="o">==</span> <span class="nf">complex</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>As you can see, it uses <code class="language-plaintext highlighter-rouge">j</code> to denote the imaginary unit, instead of <code class="language-plaintext highlighter-rouge">i</code>.</p> <p>The <strong>conjugate</strong> of a complex number is <strong>flipping the sign of the imaginary part</strong>.</p> <p>If <code class="language-plaintext highlighter-rouge">z</code> is a python <code class="language-plaintext highlighter-rouge">complex</code> number, <code class="language-plaintext highlighter-rouge">z.real</code> gets the real part (exactly as an object attribute) and <code class="language-plaintext highlighter-rouge">z.imag</code> gets the imaginary part.</p> <p>Just as complex numbers can be interpreted as a sort of wrapper around tuples of reals, a complex number data type wraps two floats. Numpy uses this to implement complex numbers of different sizes/precisions.</p> <p>The syntax is close enough to <code class="language-plaintext highlighter-rouge">cmath</code>, but it comes with the power and convenience of numpy. Importantly, other numpy methods are better at <em>casting</em> to and from complex.</p> <p>observe the following <a href="https://www.math-only-math.com/the-cube-roots-of-unity.html">cube roots of unity</a></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">z1</span> <span class="o">=</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">complex</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">3</span><span class="p">))</span> <span class="c1"># Numpy's constructor is basically the same.  
</span><span class="n">z2</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">conj</span><span class="p">(</span><span class="n">z1</span><span class="p">)</span> <span class="c1"># but numpy gives us a conjugation function, while the standard module does not. 
</span>
<span class="k">assert</span> <span class="n">math</span><span class="p">.</span><span class="nf">isclose</span><span class="p">(</span><span class="n">z1</span><span class="o">**</span><span class="mi">3</span><span class="p">,</span> <span class="n">z2</span><span class="o">**</span><span class="mi">3</span><span class="p">)</span>
<span class="c1"># TypeError: can't convert complex to float
</span>
<span class="n">np</span><span class="p">.</span><span class="n">testing</span><span class="p">.</span><span class="nf">assert_almost_equal</span><span class="p">(</span><span class="n">z1</span><span class="o">**</span><span class="mi">3</span><span class="p">,</span> <span class="n">z2</span><span class="o">**</span><span class="mi">3</span><span class="p">)</span>
</code></pre></div></div> <p><a href="https://docs.scipy.org/doc/numpy/user/basics.types.html">Read on about numpy’s complex numbers</a></p> <h4 id="dot--cross">dot &amp; cross</h4> <p>The dot <code class="language-plaintext highlighter-rouge">·</code> and cross <code class="language-plaintext highlighter-rouge">×</code> symbols have different uses depending on context.</p> <p>They might seem obvious, but it’s important to understand the subtle differences before we continue into other sections.</p> <h6 id="scalar-multiplication">scalar multiplication</h6> <p>Both symbols can represent simple multiplication of scalars. The following are equivalent:</p> <p><img src="http://latex.codecogs.com/svg.latex?5%20%5Ccdot%204%20%3D%205%20%5Ctimes%204" alt="dotcross1"/></p> <p>In programming languages we tend to use asterisk for multiplication:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">result</span> <span class="o">=</span> <span class="mi">5</span> <span class="o">*</span> <span class="mi">4</span>
</code></pre></div></div> <p>Often, the multiplication sign is only used to avoid ambiguity (e.g. between two numbers). Here, we can omit it entirely:</p> <p><img src="http://latex.codecogs.com/svg.latex?3kj" alt="dotcross2"/></p> <p>If these variables represent scalars, the code would be:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">result</span> <span class="o">=</span> <span class="mi">3</span> <span class="o">*</span> <span class="n">k</span> <span class="o">*</span> <span class="n">j</span>
</code></pre></div></div> <h6 id="vector-multiplication">vector multiplication</h6> <p>To denote multiplication of one vector with a scalar, or element-wise multiplication of a vector with another vector, we typically do not use the dot <code class="language-plaintext highlighter-rouge">·</code> or cross <code class="language-plaintext highlighter-rouge">×</code> symbols. These have different meanings in linear algebra, discussed shortly.</p> <p>Let’s take our earlier example but apply it to vectors. For element-wise vector multiplication, you might see an open dot <code class="language-plaintext highlighter-rouge">∘</code> to represent the <a href="https://en.wikipedia.org/wiki/Hadamard_product_%28matrices%29">Hadamard product</a>.<sup><a href="http://mimosa-pudica.net/improved-oren-nayar.html#images">2</a></sup></p> <p><img src="http://latex.codecogs.com/svg.latex?3%5Cmathbf%7Bk%7D%5Ccirc%5Cmathbf%7Bj%7D" alt="dotcross3"/></p> <p>In other instances, the author might explicitly define a different notation, such as a circled dot <code class="language-plaintext highlighter-rouge">⊙</code> or a filled circle <code class="language-plaintext highlighter-rouge">●</code>.<sup><a href="https://en.wikipedia.org/wiki/G%C3%B6del,_Escher,_Bach">3</a></sup></p> <p>Here is how it would look in code, using arrays <code class="language-plaintext highlighter-rouge">[x, y]</code> to represent the 2D vectors.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">s</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">k</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">]</span>
<span class="n">j</span> <span class="o">=</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">]</span>

<span class="n">tmp</span> <span class="o">=</span> <span class="nf">multiply</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">j</span><span class="p">)</span>
<span class="n">result</span> <span class="o">=</span> <span class="nf">multiply_scalar</span><span class="p">(</span><span class="n">tmp</span><span class="p">,</span> <span class="n">s</span><span class="p">)</span>
<span class="c1"># Out: [6, 18]
</span></code></pre></div></div> <p>Our <code class="language-plaintext highlighter-rouge">multiply</code> and <code class="language-plaintext highlighter-rouge">multiply_scalar</code> functions look like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">multiply</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span>
  <span class="k">return</span> <span class="p">[</span><span class="n">aa</span> <span class="o">*</span> <span class="n">bb</span> <span class="k">for</span> <span class="n">aa</span><span class="p">,</span><span class="n">bb</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">a</span><span class="p">,</span><span class="n">b</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">multiply_scalar</span><span class="p">(</span><span class="n">scalar</span><span class="p">,</span> <span class="n">a</span><span class="p">):</span>
  <span class="k">return</span> <span class="p">[</span><span class="n">scalar</span> <span class="o">*</span> <span class="n">aa</span> <span class="k">for</span> <span class="n">aa</span> <span class="ow">in</span> <span class="n">a</span><span class="p">]</span>

</code></pre></div></div> <p>Similarly, matrix multiplication typically does not use the dot <code class="language-plaintext highlighter-rouge">·</code> or cross symbol <code class="language-plaintext highlighter-rouge">×</code>.</p> <p>Numpy’s broadcasted syntax for scaling looks like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">multiply_scalar</span><span class="p">(</span><span class="n">scalar</span><span class="p">,</span> <span class="n">a</span><span class="p">):</span> 
  <span class="k">return</span> <span class="n">scalar</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">a</span><span class="p">)</span>
</code></pre></div></div> <h6 id="dot-product">dot product</h6> <p>The dot symbol <code class="language-plaintext highlighter-rouge">·</code> can be used to denote the <a href="https://en.wikipedia.org/wiki/Dot_product"><em>dot product</em></a> of two vectors. Sometimes this is called the <em>scalar product</em> since it evaluates to a scalar.</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Cmathbf%7Bk%7D%5Ccdot%20%5Cmathbf%7Bj%7D" alt="dotcross4"/></p> <p>It is a very common feature of linear algebra, and with a 3D vector it might look like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">k</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
<span class="n">j</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>

<span class="n">d</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">j</span><span class="p">)</span>
<span class="c1"># Out: 0
</span></code></pre></div></div> <p>The result <code class="language-plaintext highlighter-rouge">0</code> tells us our vectors are perpendicular. Here is a <code class="language-plaintext highlighter-rouge">dot</code> function for 3-component vectors:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">dot</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span>
  <span class="k">return</span> <span class="n">a</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">+</span> <span class="n">a</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">+</span> <span class="n">a</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>
</code></pre></div></div> <h6 id="cross-product">cross product</h6> <p>The cross symbol <code class="language-plaintext highlighter-rouge">×</code> can be used to denote the <a href="https://en.wikipedia.org/wiki/Cross_product"><em>cross product</em></a> of two vectors.</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Cmathbf%7Bk%7D%5Ctimes%20%5Cmathbf%7Bj%7D" alt="dotcross5"/></p> <p>In code, it would look like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">k</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>
<span class="n">j</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>

<span class="n">result</span> <span class="o">=</span> <span class="nf">cross</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">j</span><span class="p">)</span>
<span class="c1"># Out: [ 0, 0, -1 ]
</span></code></pre></div></div> <p>Here, we get <code class="language-plaintext highlighter-rouge">[0, 0, -1]</code>, which is perpendicular to both <strong>k</strong> and <strong>j</strong>.</p> <p>Our <code class="language-plaintext highlighter-rouge">cross</code> function:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">cross</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span> 
  <span class="sh">'''</span><span class="s"> take two 3D vectors and return their cross product. </span><span class="sh">'''</span>
  <span class="n">rx</span> <span class="o">=</span> <span class="n">a</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">-</span> <span class="n">a</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
  <span class="n">ry</span> <span class="o">=</span> <span class="n">a</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">-</span> <span class="n">a</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>
  <span class="n">rz</span> <span class="o">=</span> <span class="n">a</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">-</span> <span class="n">a</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">*</span> <span class="n">b</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
  <span class="k">return</span> <span class="n">rx</span><span class="p">,</span> <span class="n">ry</span><span class="p">,</span> <span class="n">rz</span>
</code></pre></div></div> <p>It’s good to practice and grok these operations, but in real life you’ll use Numpy.</p> <h4 id="sigma">sigma</h4> <p>The big Greek <code class="language-plaintext highlighter-rouge">Σ</code> (Sigma) is for <a href="https://en.wikipedia.org/wiki/Summation">Summation</a>. In other words: summing up some numbers.</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Csum_%7Bi%3D1%7D%5E%7B100%7Di" alt="sigma"/></p> <p>Here, <code class="language-plaintext highlighter-rouge">i=1</code> says to start at <code class="language-plaintext highlighter-rouge">1</code> and end at the number above the Sigma, <code class="language-plaintext highlighter-rouge">100</code>. These are the lower and upper bounds, respectively. The <em>i</em> to the right of the “E” tells us what we are summing. In code:</p> <p>Hence, the big sigma is the <code class="language-plaintext highlighter-rouge">for</code> keyword.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">sum</span><span class="p">([</span><span class="n">k</span> <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">100</span><span class="p">)])</span>
<span class="c1"># Out: 5050
</span></code></pre></div></div> <p><strong>Tip:</strong> With whole numbers, this particular pattern can be optimized to the following (and try to <a href="http://mathcentral.uregina.ca/QQ/database/QQ.02.06/jo1.html">grok the proof</a>. The legend of how Gauss discovered I can only describe as “typical programmer antics”):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sum_to_n</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>
  <span class="sh">'''</span><span class="s"> return the sum of integers from 0 to n</span><span class="sh">'''</span>
  <span class="k">return</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">n</span> <span class="o">*</span> <span class="p">(</span><span class="n">n</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>Here is another example where the <em>i</em>, or the “what to sum,” is different:</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Csum_%7Bi%3D1%7D%5E%7B100%7D%282i&plus;1%29" alt="sum2"/></p> <p>In code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">sum</span><span class="p">([</span><span class="mi">2</span><span class="o">*</span><span class="n">k</span> <span class="o">+</span> <span class="mi">1</span> <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">100</span><span class="p">)])</span>
<span class="c1"># Out: 10000
</span></code></pre></div></div> <p><strong>important</strong>: <code class="language-plaintext highlighter-rouge">range</code> in python has an <em>inclusive lower bound and exclusive upper bound</em>, meaning that <code class="language-plaintext highlighter-rouge">... for k in range(100)</code> is equivalent to <code class="language-plaintext highlighter-rouge">the sum of ... for k=0 to k=n</code>.</p> <p>If you’re still not absolutely fluent in indexing for these applications, spend some time with <a href="https://youtu.be/TDpQSa3hJRw">Trev Tutor</a> on youtube.</p> <p>The notation can be nested, which is much like nesting a <code class="language-plaintext highlighter-rouge">for</code> loop. You should evaluate the right-most sigma first, unless the author has enclosed them in parentheses to alter the order. However, in the following case, since we are dealing with finite sums, the order does not matter.</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Csum_%7Bi%3D1%7D%5E%7B2%7D%5Csum_%7Bj%3D4%7D%5E%7B6%7D%283ij%29" alt="sigma3"/></p> <p>In code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">sum</span><span class="p">(</span>
  <span class="p">[</span><span class="nf">sum</span><span class="p">([</span><span class="mi">3</span><span class="o">*</span><span class="n">i</span><span class="o">*</span><span class="n">j</span> 
        <span class="k">for</span> <span class="n">j</span> 
        <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span><span class="mi">7</span><span class="p">)])</span> 
   <span class="k">for</span> <span class="n">i</span> 
   <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">3</span><span class="p">)])</span>
<span class="c1"># Out: 135
</span></code></pre></div></div> <h4 id="capital-pi">capital Pi</h4> <p>The capital Pi or “Big Pi” is very similar to <a href="#sigma">Sigma</a>, except we are using multiplication to find the product of a sequence of values.</p> <p>Take the following:</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Cprod_%7Bi%3D1%7D%5E%7B6%7Di" alt="capitalPi"/></p> <p>This was removed from vanilla python for python 3, but it’s easy to recover with a generalization of the list accumulator.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">times</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">):</span> 
  <span class="sh">'''</span><span class="s"> first, give a name to the multiplication operator </span><span class="sh">'''</span>
  <span class="k">return</span> <span class="n">x</span> <span class="o">*</span> <span class="n">y</span>

<span class="kn">from</span> <span class="n">functools</span> <span class="kn">import</span> <span class="nb">reduce</span>

<span class="nf">reduce</span><span class="p">(</span><span class="n">times</span><span class="p">,</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">7</span><span class="p">))</span>
<span class="c1"># Out: 720
</span></code></pre></div></div> <p>With reduce, you can actually repeatedly apply a binary function to items of a list and accumulate the value <em>for any binary operator</em>. Python gives <code class="language-plaintext highlighter-rouge">and</code> and <code class="language-plaintext highlighter-rouge">or</code> out of the box like <code class="language-plaintext highlighter-rouge">sum</code>, but keep <code class="language-plaintext highlighter-rouge">reduce</code> in mind if you encounter a less common binary operator out in the wild.</p> <p>Note that in Numpy arrays, the syntax is different (and product is given out of the box)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="n">xs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="mi">2</span><span class="o">*</span><span class="n">k</span> <span class="o">+</span> <span class="mi">1</span> <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">100</span><span class="p">)])</span>
<span class="n">ys</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span><span class="mi">7</span><span class="p">))</span>

<span class="n">xs</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
<span class="c1"># Out: 10000
</span>
<span class="n">ys</span><span class="p">.</span><span class="nf">prod</span><span class="p">()</span>
<span class="c1"># Out: 720
</span></code></pre></div></div> <p>which is better on larger input, but you’re always welcome to use functions for ordinary lists as you please.</p> <h4 id="pipes">pipes</h4> <p>Pipe symbols, known as <em>bars</em>, can mean different things depending on the context. Below are three common uses: <a href="#absolute-value">absolute value</a>, <a href="#euclidean-norm">Euclidean norm</a>, and <a href="#determinant">determinant</a>.</p> <p>These three features all describe the <em>length</em> of an object.</p> <h6 id="absolute-value">absolute value</h6> <p><img src="http://latex.codecogs.com/svg.latex?%5Cleft%20%7C%20x%20%5Cright%20%7C" alt="pipes1"/></p> <p>For a number <em>x</em>, <code class="language-plaintext highlighter-rouge">|x|</code> means the absolute value of <em>x</em>. In code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="o">-</span><span class="mi">5</span>
<span class="nf">abs</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
<span class="c1"># Out: 5
</span></code></pre></div></div> <h6 id="euclidean-norm">Euclidean norm</h6> <p><img src="http://latex.codecogs.com/svg.latex?%5Cleft%20%5C%7C%20%5Cmathbf%7Bv%7D%20%5Cright%20%5C%7C" alt="pipes4"/></p> <p>For a vector <strong>v</strong>, <code class="language-plaintext highlighter-rouge">‖v‖</code> is the <a href="https://en.wikipedia.org/wiki/Norm_%28mathematics%29#Euclidean_norm">Euclidean norm</a> of <strong>v</strong>. It is also referred to as the “magnitude” or “length” of a vector.</p> <p>Often this is represented by double-bars to avoid ambiguity with the <em>absolute value</em> notation, but sometimes you may see it with single bars:</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Cleft%20%7C%20%5Cmathbf%7Bv%7D%20%5Cright%20%7C" alt="pipes2"/></p> <p>Here is an example using an array <code class="language-plaintext highlighter-rouge">[x, y, z]</code> to represent a 3D vector.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">v</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="o">-</span><span class="mi">3</span><span class="p">]</span>
<span class="nf">length</span><span class="p">(</span><span class="n">v</span><span class="p">)</span>
<span class="c1"># Out: 5.0
</span>
</code></pre></div></div> <p>The `length** function:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">length</span><span class="p">(</span><span class="n">vec</span><span class="p">):</span>
  <span class="n">x</span> <span class="o">=</span> <span class="n">vec</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
  <span class="n">y</span> <span class="o">=</span> <span class="n">vec</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
  <span class="n">z</span> <span class="o">=</span> <span class="n">vec</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>
  <span class="k">return</span> <span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">x</span><span class="o">**</span><span class="mi">2</span> <span class="o">+</span> <span class="n">y</span><span class="o">**</span><span class="mi">2</span> <span class="o">+</span> <span class="n">z</span><span class="o">**</span><span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <p>The implementation for arbitrary length’d vectors is left as an exercise for the reader.</p> <p>In practice, you’ll probably use the following numpy call</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">norm</span><span class="p">([</span><span class="mi">0</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="o">-</span><span class="mi">3</span><span class="p">])</span>
<span class="c1"># Out: 5.0
</span></code></pre></div></div> <p>Resources:</p> <ul> <li><a href="get link to numpy.linalg docs">numpy.linalg docs</a></li> </ul> <h6 id="determinant">determinant</h6> <p><img src="http://latex.codecogs.com/svg.latex?%5Cleft%20%7C%5Cmathbf%7BA%7D%20%5Cright%20%7C" alt="pipes3"/></p> <p>For a matrix <strong>A</strong>, <code class="language-plaintext highlighter-rouge">|A|</code> means the <a href="https://en.wikipedia.org/wiki/Determinant">determinant</a> of matrix <strong>A</strong>.</p> <p>Here is an example computing the determinant of a 2x2 identity matrix</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ident_2</span> <span class="o">=</span> <span class="p">[[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">],</span> 
           <span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">]]</span>

<span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">det</span><span class="p">(</span><span class="n">ident_2</span><span class="p">)</span>
<span class="c1"># Out: 1
</span></code></pre></div></div> <p>You should watch <a href="https://www.youtube.com/playlist?list=PLZHQObOWTQDPD3MizzM2xVFitgF8hE_ab">3blue1brown</a>, but in short if a matrix (list of list of numbers) is interpreted as hitting a <strong>coordinate system</strong> with a <em>squisher-stretcher-rotater</em>, the determinant of that matrix is the <strong>measure of how much the unit area/volume of the coordinate system got squished-stretched-rotated</strong>.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">det</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">identity</span><span class="p">(</span><span class="mi">100</span><span class="p">))</span> <span class="c1"># the determinant of the 100 x 100 identity matrix is still one, because the identity matrix doesn't squish, stretch, or rotate at all. 
# Out: 1.0
</span>
<span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">det</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([[</span><span class="mi">0</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">],</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">]]))</span> <span class="c1"># 90 degree rotation. 
# Out: 1.0
</span>
</code></pre></div></div> <p>The second matrix was the <a href="https://en.wikipedia.org/wiki/Rotation_matrix"><strong>2D rotation</strong></a> at 90 degrees.</p> <h4 id="hat">hat</h4> <p>In geometry, the “hat” symbol above a character is used to represent a <a href="https://en.wikipedia.org/wiki/Unit_vector">unit vector</a>. For example, here is the unit vector of <strong>a</strong>:</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Chat%7B%5Cmathbf%7Ba%7D%7D" alt="hat"/></p> <p>In Cartesian space, a unit vector is typically length 1. That means each part of the vector will be in the range of -1.0 to 1.0. Here we <em>normalize</em> a 3D vector into a unit vector:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">a</span> <span class="o">=</span> <span class="p">[</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="o">-</span><span class="mi">3</span> <span class="p">]</span>
<span class="nf">normalize</span><span class="p">(</span><span class="n">a</span><span class="p">)</span>
<span class="c1"># Out: [ 0, 0.8, -0.6 ]
</span></code></pre></div></div> <p>If a vector is that which has magnitude and direction, normalization of a vector is the operation that deletes magnitude and preserves direction.</p> <p>Here is the <code class="language-plaintext highlighter-rouge">normalize</code> function, operating on 3D vectors:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">normalize</span><span class="p">(</span><span class="n">vec</span><span class="p">):</span>
  <span class="n">x</span> <span class="o">=</span> <span class="n">vec</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
  <span class="n">y</span> <span class="o">=</span> <span class="n">vec</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
  <span class="n">z</span> <span class="o">=</span> <span class="n">vec</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span>
  <span class="n">squaredLength</span> <span class="o">=</span> <span class="n">x</span> <span class="o">*</span> <span class="n">x</span> <span class="o">+</span> <span class="n">y</span> <span class="o">*</span> <span class="n">y</span> <span class="o">+</span> <span class="n">z</span> <span class="o">*</span> <span class="n">z</span>

  <span class="nf">if </span><span class="p">(</span><span class="n">squaredLength</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">):</span>
    <span class="n">length</span> <span class="o">=</span> <span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">squaredLength</span><span class="p">)</span>
    <span class="n">vec</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">=</span> <span class="n">x</span> <span class="o">/</span> <span class="n">length</span>
    <span class="n">vec</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">=</span> <span class="n">y</span> <span class="o">/</span> <span class="n">length</span>
    <span class="n">vec</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="o">=</span> <span class="n">z</span> <span class="o">/</span> <span class="n">length</span>
  
  <span class="k">return</span> <span class="n">vec</span>
</code></pre></div></div> <p>Which Numpy’s <strong>broadcasting</strong> syntax sugar can do in fewer lines</p> <p>You should try to generalize this to vectors of arbitrary length yourself, before reading this…</p> <p>Go, I mean it!</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">normalize</span><span class="p">(</span><span class="n">vec</span><span class="p">):</span>
  <span class="sh">'''</span><span class="s"> *sigh* if you don</span><span class="sh">'</span><span class="s">t do it yourself you</span><span class="sh">'</span><span class="s">ll never learn! </span><span class="sh">'''</span>
  <span class="n">vec</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">vec</span><span class="p">)</span> <span class="c1"># ensure that input is casted to numpy
</span>  <span class="n">length</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">vec</span><span class="p">)</span>
  <span class="k">if</span> <span class="n">length</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
    <span class="k">return</span> <span class="n">vec</span> <span class="o">/</span> <span class="n">length</span>
</code></pre></div></div> <p>Notice that <strong>broadcasting</strong> here is just short for <code class="language-plaintext highlighter-rouge">[x / length for x in vec]</code>. But it’s actually <strong>faster</strong> on large input, because arrays.</p> <p><a href="https://docs.scipy.org/doc/numpy/reference/routines.linalg.html"><em>Read</em> the Numpy docs. <em>BE</em> the Numpy docs</a></p> <h4 id="element">element</h4> <p>In set theory, the “element of” symbol <code class="language-plaintext highlighter-rouge">∈</code> and <code class="language-plaintext highlighter-rouge">∋</code> can be used to describe whether something is an element of a <em>set</em>. For example:</p> <p><img src="http://latex.codecogs.com/svg.latex?A%3D%5Cleft%20%5C%7B3%2C9%2C14%7D%7B%20%5Cright%20%5C%7D%2C%203%20%5Cin%20A" alt="element1"/></p> <p>Here we have a set of numbers <em>A</em> = <code class="language-plaintext highlighter-rouge">{ 3, 9, 14 }</code> and we are saying <code class="language-plaintext highlighter-rouge">3</code> is an “element of” that set.</p> <p>The <code class="language-plaintext highlighter-rouge">in</code> keyword plays the role of the elementhood function, giving a bool.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">A</span> <span class="o">=</span> <span class="p">[</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">14</span> <span class="p">]</span>

<span class="mi">3</span> <span class="ow">in</span> <span class="n">A</span>
<span class="c1"># Out: True
</span></code></pre></div></div> <p>Python also has <code class="language-plaintext highlighter-rouge">set</code>. You can wrap any iterable or generator with the <code class="language-plaintext highlighter-rouge">set</code> keyword to delete repeats.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">set</span><span class="p">([</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">,</span><span class="mi">1</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">4</span><span class="p">,</span><span class="mi">5</span><span class="p">,</span><span class="mi">3</span><span class="p">])</span>
<span class="c1"># Out: {1, 2, 3, 4, 5}
</span>
<span class="mi">3</span> <span class="ow">in</span> <span class="nf">set</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">20</span><span class="p">,</span> <span class="mi">4</span><span class="p">))</span>
<span class="c1"># Out: False
</span></code></pre></div></div> <p>The backwards <code class="language-plaintext highlighter-rouge">∋</code> is the same, but the order changes:</p> <p><img src="http://latex.codecogs.com/svg.latex?A%3D%5Cleft%20%5C%7B3%2C9%2C14%7D%7B%20%5Cright%20%5C%7D%2C%20A%20%5Cni%203" alt="element2"/></p> <p>You can also use the “not an element of” symbols <code class="language-plaintext highlighter-rouge">∉</code> and <code class="language-plaintext highlighter-rouge">∌</code> like so:</p> <p><img src="http://latex.codecogs.com/svg.latex?A%3D%5Cleft%20%5C%7B3%2C9%2C14%7D%7B%20%5Cright%20%5C%7D%2C%206%20%5Cnotin%20A" alt="element3"/></p> <p>Which you know is represented by the convenient <code class="language-plaintext highlighter-rouge">not</code> keyword in python.</p> <h4 id="common-number-sets">common number sets</h4> <p>You may see some some large <a href="https://en.wikipedia.org/wiki/Blackboard_bold">Blackboard</a> letters among equations. Often, these are used to describe sets.</p> <p>For example, we might describe <em>k</em> to be an <a href="#element">element of</a> the set <code class="language-plaintext highlighter-rouge">ℝ</code>.</p> <p><img src="http://latex.codecogs.com/svg.latex?k%20%5Cin%20%5Cmathbb%7BR%7D" alt="real"/></p> <p>Listed below are a few common sets and their symbols.</p> <h6 id="ℝ-real-numbers"><code class="language-plaintext highlighter-rouge">ℝ</code> real numbers</h6> <p>The large <code class="language-plaintext highlighter-rouge">ℝ</code> describes the set of <em>real numbers</em>. These include integers, as well as rational and irrational numbers.</p> <p>Computers approximate <code class="language-plaintext highlighter-rouge">ℝ</code> with <code class="language-plaintext highlighter-rouge">float</code>.</p> <p>You can use <code class="language-plaintext highlighter-rouge">isinstance</code> to check “<em>k</em> ∈ ℝ”, where float and <code class="language-plaintext highlighter-rouge">ℝ</code> aren’t <em>really</em> the same thing but the intuition is close enough.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">isinstance</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="p">,</span> <span class="nb">float</span><span class="p">)</span>
<span class="c1"># Out: True
</span></code></pre></div></div> <p>Again, you may elevate that bool to an <code class="language-plaintext highlighter-rouge">assertion</code> that makes-or-breaks the whole program with the <code class="language-plaintext highlighter-rouge">assert</code> keyword when you see fit.</p> <p><a href="https://youtu.be/zguLmgYWhM0">Excellent resource on floats in python</a></p> <h6 id="ℚ-rational-numbers"><code class="language-plaintext highlighter-rouge">ℚ</code> rational numbers</h6> <p>Rational numbers are real numbers that can be expressed as a fraction, or <em>ratio</em>. Rational numbers cannot have zero as a denominator.</p> <p>Imagine taking <code class="language-plaintext highlighter-rouge">ℝ</code> and removing radicals (like <code class="language-plaintext highlighter-rouge">np.sqrt</code>) and logarithms (in a family called <a href="https://en.wikipedia.org/wiki/Transcendental_function">transcendentals</a>), that’s basically what <code class="language-plaintext highlighter-rouge">ℚ</code> is, at least enough for a rough first approximation.</p> <p>This also means that all integers are rational numbers, since the denominator can be expressed as 1.</p> <p>An irrational number, on the other hand, is one that cannot be expressed as a ratio, like π (<code class="language-plaintext highlighter-rouge">math.pi</code>).</p> <p>A reason a programmer might care about the difference between Q and R is in the design of unit tests— <em>fractions are terminating decimals</em>, and sometimes when you’re a 100% sure that a number will be a basic rational (like counting change, <code class="language-plaintext highlighter-rouge">0.25, 0.10, 0.05</code>, etc.), you’re allowed to use <code class="language-plaintext highlighter-rouge">==</code> in unit tests rather than <code class="language-plaintext highlighter-rouge">isclose</code> or <code class="language-plaintext highlighter-rouge">assert_almost_equal</code>. The point is that you know not to use exact equality <code class="language-plaintext highlighter-rouge">==</code> when anything like <code class="language-plaintext highlighter-rouge">sqrt</code> or <code class="language-plaintext highlighter-rouge">log</code> is involved!</p> <p>You can work with rationals without dividing them into floatiness with the <a href="https://docs.python.org/3.7/library/fractions.html"><code class="language-plaintext highlighter-rouge">fractions</code> standard module</a></p> <h6 id="ℤ-integers"><code class="language-plaintext highlighter-rouge">ℤ</code> integers</h6> <p>An integer is a whole number. Just imagine starting from zero and one and building out an inventory with addition and subtraction.</p> <p>An integer has no division, no decimals.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">assert</span> <span class="nf">isinstance</span><span class="p">(</span><span class="mi">8</span><span class="o">/</span><span class="mi">7</span><span class="p">,</span> <span class="nb">int</span><span class="p">),</span> <span class="sh">"</span><span class="s">GO DIRECTLY TO JAIL</span><span class="sh">"</span>
</code></pre></div></div> <h6 id="ℕ-natural-numbers"><code class="language-plaintext highlighter-rouge">ℕ</code> natural numbers</h6> <p>A natural number, a non-negative integer.</p> <p>This is actually the only set invented by the <a href="https://www.brainyquote.com/quotes/leopold_kronecker_338745">flying spaghetti monster</a>: as for the others, humans have themselves to blame.</p> <p>Depending on the context and field of study, the set may or may not <strong>start with zero</strong>.</p> <p>…ok but, between you and me, <strong>they 200% start with zero</strong>.</p> <p><code class="language-plaintext highlighter-rouge">ℕ</code> also happens to be the first <strong>inductive construction</strong> in the <a href="https://en.wikipedia.org/wiki/Semantics_(computer_science)">study of datatypes</a>, consisting of a single axiom (“Zero is a <code class="language-plaintext highlighter-rouge">ℕ</code>”) and a single inference rule (“if <code class="language-plaintext highlighter-rouge">n</code> is a <code class="language-plaintext highlighter-rouge">ℕ</code> then <code class="language-plaintext highlighter-rouge">n + 1</code> is also a <code class="language-plaintext highlighter-rouge">ℕ</code>”)</p> <p><code class="language-plaintext highlighter-rouge">ℕ</code> is not a datatype in python, we can’t use <em>typechecking</em> to disambiguate <code class="language-plaintext highlighter-rouge">int</code> from <code class="language-plaintext highlighter-rouge">non-negative int</code>, but in a pinch you could easily write up something that combines <code class="language-plaintext highlighter-rouge">x &gt;= 0</code> judgments with <code class="language-plaintext highlighter-rouge">isinstance(x, int)</code>.</p> <h6 id="ℂ-complex-numbers"><code class="language-plaintext highlighter-rouge">ℂ</code> complex numbers</h6> <p>As we saw earlier, the complex numbers are a particular wrapper around tuples of reals.</p> <p>A complex number is a combination of a real and imaginary number, viewed as a co-ordinate in the 2D plane. For more info, see <a href="http://betterexplained.com/articles/a-visual-intuitive-guide-to-imaginary-numbers/">A Visual, Intuitive Guide to Imaginary Numbers</a>.</p> <p>We can say <code class="language-plaintext highlighter-rouge">ℂ = {a + b*i | a,b ∈ ℝ}</code>, which is a notation called</p> <h4 id="set-builder-notation">Set builder notation</h4> <p>Pythoners have a name for <em>set builder notation</em>; and the name is comprehension</p> <ul> <li><code class="language-plaintext highlighter-rouge">{ }</code>: delimiter around iterable (curlybois for <code class="language-plaintext highlighter-rouge">dict</code> or <code class="language-plaintext highlighter-rouge">set</code>, <code class="language-plaintext highlighter-rouge">[</code> for list)</li> <li><code class="language-plaintext highlighter-rouge">a + b * i</code>: an expression (for instance, earlier when we made a list of odd numbers this expression was <code class="language-plaintext highlighter-rouge">2*k + 1</code>) to be evaluated for each item in source list.</li> <li><code class="language-plaintext highlighter-rouge">|</code>: <code class="language-plaintext highlighter-rouge">for</code></li> <li><code class="language-plaintext highlighter-rouge">a,b ∈ ℝ</code>: this just shows that <code class="language-plaintext highlighter-rouge">a,b</code> are drawn from a particular place, in this case the real numbers.</li> </ul> <p>So if you’ve been writing Python listcomps, that definition of the complex numbers wasn’t so bad! Say it with me this time</p> <p><code class="language-plaintext highlighter-rouge">ℂ = {a + b*i | a,b ∈ ℝ}</code>`</p> <p><strong>inhaaaaaaless</strong> <em>unison</em> “C IS THE SET OF a + b*i FOR REAL NUMBERS a AND b”</p> <p>If you want, you can draw up a grainy picture of an <em>interval</em> of ℂ with <code class="language-plaintext highlighter-rouge">zip</code> and <code class="language-plaintext highlighter-rouge">np.linspace</code>, and of course list comprehension.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">j</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">complex</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mi">1</span><span class="p">)</span>

<span class="n">R</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">100</span><span class="p">)</span>

<span class="p">{</span><span class="n">a</span> <span class="o">+</span> <span class="n">b</span> <span class="o">*</span> <span class="n">j</span> <span class="k">for</span> <span class="n">a</span><span class="p">,</span><span class="n">b</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">R</span><span class="p">,</span> <span class="n">R</span><span class="p">)}</span>
<span class="c1"># too much to print but try it yourself. 
</span></code></pre></div></div> <h4 id="functions">functions</h4> <p><a href="https://en.wikipedia.org/wiki/Function_%28mathematics%29">Functions</a> are fundamental features of mathematics, and the concept is fairly easy to translate into code.</p> <p>A <strong>function</strong> transforms an input into an output value. For example, the following is a function:</p> <p><img src="http://latex.codecogs.com/svg.latex?x%5E%7B2%7D" alt="function1"/></p> <p>We can give this function a <em>name</em>. Commonly, we use <code class="language-plaintext highlighter-rouge">ƒ</code> to describe a function, but it could be named <code class="language-plaintext highlighter-rouge">A</code> or anything else.</p> <p><img src="http://latex.codecogs.com/svg.latex?f%5Cleft%20%28x%20%5Cright%20%29%20%3D%20x%5E%7B2%7D" alt="function2"/></p> <p>In code, we might name it <code class="language-plaintext highlighter-rouge">square</code> and write it like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">square</span><span class="p">(</span><span class="n">x</span><span class="p">):</span> 
  <span class="k">return</span> <span class="n">math</span><span class="p">.</span><span class="nf">pow</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>

</code></pre></div></div> <p>Sometimes a function is not named, and instead the output is written.</p> <p><img src="http://latex.codecogs.com/svg.latex?y%20%3D%20x%5E%7B2%7D" alt="function3"/></p> <p>In the above example, <em>x</em> is the input, the transformation is <em>squaring</em>, and <em>y</em> is the output. We can express this as an equation because, conventionally, we think of <em>x</em> as input and <em>y</em> as output.</p> <p>But we have a stronger idea called <strong>anonymous functions</strong> to generalize this.</p> <p>Just as we can name strings <code class="language-plaintext highlighter-rouge">x = "Alonzo"</code> then call them with their names <em>or</em> we can just pass string <em>literals</em>, we also have <strong>function literals</strong>.</p> <p>Math first, then python:</p> <p><code class="language-plaintext highlighter-rouge">x ↦ x^2</code> is equivalent to the equational description above.</p> <p>Nearly identical, but very different to the untrained eye, is <code class="language-plaintext highlighter-rouge">λx.x^2</code>, hence the python keyword</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="o">**</span><span class="mi">2</span>
</code></pre></div></div> <p>Functions can also have multiple parameters, like in a programming language. These are known as <em>arguments</em> in mathematics, and the number of arguments a function takes is known as the <em>arity</em> of the function.</p> <p><img src="http://latex.codecogs.com/svg.latex?f%28x%2Cy%29%20%3D%20%5Csqrt%7Bx%5E2%20&plus;%20y%5E2%7D" alt="function4"/></p> <h5 id="dictionaries-are-functions">dictionaries are functions</h5> <p>Sometimes mathematicians, like software developers, need to specify <em>maps</em> by *enumerating each input-output pair** when there is no expression that computes output from input.</p> <p><strong>Note</strong>: formally, mathematicians require that <strong>functions not be ambiguous</strong><em>, so when you have a function and you have an input, there can be no uncertainty as to what the output should be; you mustn’t be confused about whether an apple is red or purple (in introductory algebra courses this is called the “vertical line test”, but it applies to all maps). Notice that the implementation of hash maps already guarantees this in the case of dictionaries! Notice also that we make no such requirement on *outputs</em>, both an apple <em>and</em> a banana can land on purple! With caveats like these, we can <a href="https://en.wikipedia.org/wiki/Bijection,_injection_and_surjection">study the properties of different kinds of functions</a> into different kinds, important in compression and security engineering.</p> <h5 id="piecewise-function">piecewise function</h5> <p>Some functions will use different relationships depending on the input value, <em>x</em>.</p> <p>The following function <em>ƒ</em> chooses between two “sub functions” depending on the input value.</p> <p><img src="http://latex.codecogs.com/svg.latex?f%28x%29%3D%20%5Cbegin%7Bcases%7D%20%5Cfrac%7Bx%5E2-x%7D%7Bx%7D%2C%26%20%5Ctext%7Bif%20%7D%20x%5Cgeq%201%5C%5C%200%2C%20%26%20%5Ctext%7Botherwise%7D%20%5Cend%7Bcases%7D" alt="piecewise1"/></p> <p>This is very similar to <code class="language-plaintext highlighter-rouge">if</code> / <code class="language-plaintext highlighter-rouge">else</code> in code. The right-side conditions are often written as <strong>“for x &lt; 0”</strong> or <strong>“if x = 0”</strong>. If the condition is true, the function to the left is used.</p> <p>In piecewise functions, <strong>“otherwise”</strong> and <strong>“elsewhere”</strong> are analogous to the <code class="language-plaintext highlighter-rouge">else</code> statement in code.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
  <span class="nf">if </span><span class="p">(</span><span class="n">x</span> <span class="o">&gt;=</span> <span class="mi">1</span><span class="p">):</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">pow</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="o">-</span> <span class="n">x</span><span class="p">)</span> <span class="o">/</span> <span class="n">x</span>
  <span class="k">else</span><span class="p">:</span>
    <span class="k">return</span> <span class="mi">0</span>

</code></pre></div></div> <h5 id="common-functions">common functions</h5> <p>There are some function names that are ubiquitous in mathematics. For a programmer, these might be analogous to functions “built-in” to the language (like <code class="language-plaintext highlighter-rouge">parseInt</code> in JavaScript).</p> <p>One such example is the <em>sgn</em> function. This is the <em>signum</em> or <em>sign</em> function. Let’s use <a href="#piecewise-function">piecewise function</a> notation to describe it:</p> <p><img src="http://latex.codecogs.com/svg.latex?sgn%28x%29%20%3A%3D%20%5Cbegin%7Bcases%7D%20-1%26%20%5Ctext%7Bif%20%7D%20x%20%3C%200%5C%5C%200%2C%20%26%20%5Ctext%7Bif%20%7D%20%7Bx%20%3D%200%7D%5C%5C%201%2C%20%26%20%5Ctext%7Bif%20%7D%20x%20%3E%200%5C%5C%20%5Cend%7Bcases%7D" alt="sgn"/></p> <p>In code, it might look like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">signum</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
  <span class="nf">if </span><span class="p">(</span><span class="n">x</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">):</span>
    <span class="k">return</span> <span class="o">-</span><span class="mi">1</span>
  <span class="nf">elif </span><span class="p">(</span><span class="n">x</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">):</span>
    <span class="k">return</span> <span class="mi">1</span>
  <span class="k">else</span><span class="p">:</span> 
    <span class="k">return</span> <span class="mi">0</span>
</code></pre></div></div> <p>See <a href="https://github.com/scijs/signum">signum</a> for this function as a module.</p> <p>Other examples of such functions: <em>sin</em>, <em>cos</em>, <em>tan</em>.</p> <h5 id="function-notation">function notation</h5> <p>In some literature, functions may be defined with more explicit notation. For example, let’s go back to the <code class="language-plaintext highlighter-rouge">square</code> function we mentioned earlier:</p> <p><img src="http://latex.codecogs.com/svg.latex?f%5Cleft%20%28x%20%5Cright%20%29%20%3D%20x%5E%7B2%7D" alt="function2"/></p> <p>It might also be written in the following form:</p> <p><img src="http://latex.codecogs.com/svg.latex?f%20%3A%20x%20%5Cmapsto%20x%5E2" alt="mapsto"/></p> <p>The arrow here with a tail typically means “maps to,” as in <em>x maps to x<sup>2</sup></em>.</p> <p>Sometimes, when it isn’t obvious, the notation will also describe the <em>domain</em> and <em>codomain</em> of the function. A more formal definition of <em>ƒ</em> might be written as:</p> <p><img src="http://latex.codecogs.com/svg.latex?%5Cbegin%7Balign*%7D%20f%20%3A%26%5Cmathbb%7BR%7D%20%5Crightarrow%20%5Cmathbb%7BR%7D%5C%5C%20%26x%20%5Cmapsto%20x%5E2%20%5Cend%7Balign*%7D" alt="funcnot"/></p> <p>A function’s <em>domain</em> and <em>codomain</em> is a bit like its <em>input</em> and <em>output</em> types, respectively. Here’s another example, using our earlier <em>sgn</em> function, which outputs an integer:</p> <p><img src="http://latex.codecogs.com/svg.latex?sgn%20%3A%20%5Cmathbb%7BR%7D%20%5Crightarrow%20%5Cmathbb%7BZ%7D" alt="domain2"/></p> <p>The arrow here (without a tail) is used to map one <em>set</em> to another.</p> <p>In Python and other dynamically typed languages, you might use documentation and/or runtime checks to explain and validate a function’s input/output. Example:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">square_ints</span><span class="p">(</span><span class="n">k</span><span class="p">):</span> 
  <span class="sh">'''</span><span class="s"> FEED ME INTEGER </span><span class="sh">'''</span>
  <span class="k">try</span><span class="p">:</span> 
    <span class="k">assert</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="nb">int</span><span class="p">),</span> <span class="sh">"</span><span class="s">I HUNGER FOR AN INTEGER! </span><span class="sh">"</span>
    <span class="k">return</span> <span class="n">math</span><span class="p">.</span><span class="nf">pow</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
  <span class="k">except</span> <span class="nb">AssertionError</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
    <span class="k">raise</span> <span class="n">e</span>
</code></pre></div></div> <p>The python of a more glorious future as described in <a href="https://www.python.org/dev/peps/pep-0484/">pep484</a> proposes a static type checker for Python, but no one’s proposed anything shrewd enough to <em>prevent code with type errors from compiling</em> for Python yet.</p> <p>As we will see in the following sample of <code class="language-plaintext highlighter-rouge">pep484</code> Python, the set interpretation of domain and codomain makes way for a <em>types</em> interpretation of domain and codomain</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">square</span><span class="p">(</span><span class="n">x</span><span class="p">:</span> <span class="nb">float</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span> 
  <span class="sh">'''</span><span class="s"> a pep484 annotation isn</span><span class="sh">'</span><span class="s">t that different from if i declared in the docstring; 
  
  input/domain: a float
  output/codomain: another float 
  </span><span class="sh">'''</span>
  <span class="k">return</span> <span class="n">x</span><span class="o">**</span><span class="mi">2</span>
</code></pre></div></div> <p>Other languages, like Java, allow for true method overloading based on the static types of a function’s input/output. This is closer to mathematics: two functions are not the same if they use a different <em>domain</em>. This is also called <em>polymorphism</em> and it explains why <code class="language-plaintext highlighter-rouge">'literally' + 'alonzo'</code> concats two strings together but <code class="language-plaintext highlighter-rouge">1 + 1</code> is addition on numbers.</p> <h4 id="prime">prime</h4> <p>The prime symbol (<code class="language-plaintext highlighter-rouge">′</code>) is often used in variable names to describe things which are similar, without giving it a different name altogether. It can describe the “next value” after some transformation.</p> <p>For example, if we take a 2D point <em>(x, y)</em> and rotate it, you might name the result <em>(x′, y′)</em>. Or, the <em>transpose</em> of matrix <strong>M</strong> might be named <strong>M′</strong>.</p> <p>In code, we typically just assign the variable a more descriptive name, like <code class="language-plaintext highlighter-rouge">transformedPosition</code>.</p> <p>For a mathematical <a href="#function">function</a>, the prime symbol often describes the <em>derivative</em> of that function. Derivatives will be explained in a future section. Let’s take our earlier function:</p> <p><img src="http://latex.codecogs.com/svg.latex?f%5Cleft%20%28x%20%5Cright%20%29%20%3D%20x%5E%7B2%7D" alt="function2"/></p> <p>Its derivative could be written with a prime <code class="language-plaintext highlighter-rouge">′</code> symbol:</p> <p><img src="http://latex.codecogs.com/svg.latex?f%27%28x%29%20%3D%202x" alt="prime1"/></p> <p>In code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">):</span> 
  <span class="k">return</span> <span class="n">Math</span><span class="p">.</span><span class="nf">pow</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">f_prime</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
  <span class="k">return</span> <span class="mi">2</span> <span class="o">*</span> <span class="n">x</span>
</code></pre></div></div> <p>Multiple prime symbols can be used to describe the second derivative <em>ƒ′′</em> and third derivative <em>ƒ′′′</em>. After this, authors typically express higher orders with roman numerals <em>ƒ</em><sup>IV</sup> or superscript numbers <em>ƒ</em><sup>(n)</sup>.</p> <h4 id="floor--ceiling">floor &amp; ceiling</h4> <p>The special brackets <code class="language-plaintext highlighter-rouge">⌊x⌋</code> and <code class="language-plaintext highlighter-rouge">⌈x⌉</code> represent the <em>floor</em> and <em>ceil</em> functions, respectively.</p> <p><img src="http://latex.codecogs.com/svg.latex?floor%28x%29%20%3D%20%5Clfloor%20x%20%5Crfloor" alt="floor"/></p> <p><img src="http://latex.codecogs.com/svg.latex?ceil%28x%29%20%3D%20%5Clceil%20x%20%5Crceil" alt="ceil"/></p> <p>In code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">math</span><span class="p">.</span><span class="nf">floor</span><span class="p">(</span><span class="mf">4.8</span><span class="p">)</span>
<span class="n">math</span><span class="p">.</span><span class="nf">ceil</span><span class="p">(</span><span class="mf">3.1</span><span class="p">)</span>
<span class="n">np</span><span class="p">.</span><span class="nf">floor</span><span class="p">(</span><span class="mf">4.9</span><span class="p">)</span>
<span class="n">np</span><span class="p">.</span><span class="nf">ceil</span><span class="p">(</span><span class="mf">3.001</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Note</strong>: the Numpy version returns a float, in the above example <code class="language-plaintext highlighter-rouge">4.0</code>, rather than the int <code class="language-plaintext highlighter-rouge">4</code></p> </blockquote> <p>When the two symbols are mixed <code class="language-plaintext highlighter-rouge">⌊x⌉</code>, it typically represents a function that rounds to the nearest integer:</p> <p><img src="http://latex.codecogs.com/svg.latex?round%28x%29%20%3D%20%5Clfloor%20x%20%5Crceil" alt="round"/></p> <p>Python automatically gives you a keyword <code class="language-plaintext highlighter-rouge">round</code> to call on a number.</p> <h4 id="arrows">arrows</h4> <p>Arrows are often used in <a href="#function-notation">function notation</a>. Here are a few other areas you might see them.</p> <h6 id="material-implication">material implication</h6> <p>Arrows like <code class="language-plaintext highlighter-rouge">⇒</code> and <code class="language-plaintext highlighter-rouge">→</code> are sometimes used in logic for <em>material implication.</em> That is, if A is true, then B is also true.</p> <p><img src="http://latex.codecogs.com/svg.latex?A%20%5CRightarrow%20B" alt="material1"/></p> <p>Interpreting this as code might look like this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">if_A_then_B</span><span class="p">:</span>
  <span class="k">if</span> <span class="n">A</span><span class="p">:</span>
    <span class="k">assert</span> <span class="n">B</span><span class="p">,</span> <span class="sh">"</span><span class="s">alas, not A!</span><span class="sh">"</span>
    <span class="k">return</span> <span class="n">B</span>
</code></pre></div></div> <p>The arrows can go in either direction <code class="language-plaintext highlighter-rouge">⇐</code> <code class="language-plaintext highlighter-rouge">⇒</code>, or both <code class="language-plaintext highlighter-rouge">⇔</code>. When <em>A ⇒ B</em> and <em>B ⇒ A</em>, they are said to be equivalent:</p> <p><img src="http://latex.codecogs.com/svg.latex?A%20%5CLeftrightarrow%20B" alt="material-equiv"/></p> <h6 id="inequality">inequality</h6> <p>In math, the <code class="language-plaintext highlighter-rouge">&lt;</code> <code class="language-plaintext highlighter-rouge">&gt;</code> <code class="language-plaintext highlighter-rouge">≤</code> and <code class="language-plaintext highlighter-rouge">≥</code> are typically used in the same way we use them in code: <em>less than</em>, <em>greater than</em>, <em>less than or equal to</em> and <em>greater than or equal to</em>, respectively.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">assert</span> <span class="mi">50</span> <span class="o">&gt;</span> <span class="mi">2</span>
<span class="k">assert</span> <span class="mi">2</span> <span class="o">&lt;</span> <span class="mi">10</span>
<span class="k">assert</span> <span class="mi">3</span> <span class="o">&lt;=</span> <span class="mi">4</span>
<span class="k">assert</span> <span class="mi">4</span> <span class="o">&gt;=</span> <span class="mi">4</span>
</code></pre></div></div> <p>On rare occasions you might see a slash through these symbols, to describe <em>not</em>. As in, <em>k</em> is “not greater than” <em>j</em>.</p> <p><img src="http://latex.codecogs.com/svg.latex?k%20%5Cngtr%20j" alt="ngt"/></p> <p>The <code class="language-plaintext highlighter-rouge">≪</code> and <code class="language-plaintext highlighter-rouge">≫</code> are sometimes used to represent <em>significant</em> inequality. That is, <em>k</em> is an <a href="https://en.wikipedia.org/wiki/Order_of_magnitude">order of magnitude</a> larger than <em>j</em>. Sometimes read “beats”, when I say <code class="language-plaintext highlighter-rouge">x^k ≫ log(x)</code> what I’m really saying is that “polynomial functions grow an order of magnitude faster than logarithms; in a word, the polynomial <em>beats</em> the logarithm.”</p> <p><img src="http://latex.codecogs.com/svg.latex?k%20%5Cgg%20j" alt="orderofmag"/></p> <h6 id="conjunction--disjunction">conjunction &amp; disjunction</h6> <p>Another use of arrows in logic is conjunction <code class="language-plaintext highlighter-rouge">∧</code> and disjunction <code class="language-plaintext highlighter-rouge">∨</code>. They are analogous to a programmer’s <code class="language-plaintext highlighter-rouge">AND</code> and <code class="language-plaintext highlighter-rouge">OR</code> operators, respectively.</p> <p>The following shows conjunction <code class="language-plaintext highlighter-rouge">∧</code>, the logical <code class="language-plaintext highlighter-rouge">AND</code>.</p> <p><img src="http://latex.codecogs.com/svg.latex?k%20%3E%202%20%5Cland%20k%20%3C%204%20%5CLeftrightarrow%20k%20%3D%203" alt="and"/></p> <p>In Python, we just say <code class="language-plaintext highlighter-rouge">and</code>. Assuming <em>k</em> is a natural number, the logic implies that <em>k</em> is 3:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">lambda</span> <span class="n">k</span><span class="p">:</span> <span class="nf">if </span><span class="p">(</span><span class="n">k</span> <span class="o">&gt;</span> <span class="mi">2</span> <span class="ow">and</span> <span class="n">k</span> <span class="o">&lt;</span> <span class="mi">4</span><span class="p">):</span> <span class="k">assert</span> <span class="n">k</span> <span class="o">==</span> <span class="mi">3</span><span class="p">,</span> <span class="sh">"</span><span class="s">Exercise: can this error ever be raised?</span><span class="sh">"</span>
</code></pre></div></div> <p>Since both sides are equivalent <code class="language-plaintext highlighter-rouge">⇔</code>, it also implies the following:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">lambda</span> <span class="n">k</span><span class="p">:</span> <span class="nf">if </span><span class="p">(</span><span class="n">k</span> <span class="o">==</span> <span class="mi">3</span><span class="p">):</span> <span class="nf">assert </span><span class="p">(</span><span class="n">k</span> <span class="o">&gt;</span> <span class="mi">2</span> <span class="ow">and</span> <span class="n">k</span> <span class="o">&lt;</span> <span class="mi">4</span><span class="p">),</span> <span class="sh">"</span><span class="s">I mean it, think through this exercise.</span><span class="sh">"</span>
</code></pre></div></div> <p>The down arrow <code class="language-plaintext highlighter-rouge">∨</code> is logical disjunction, like the OR operator.</p> <p><img src="http://latex.codecogs.com/svg.latex?A%20%5Clor%20B" alt="logic-or"/></p> <p>In Python, we have the <code class="language-plaintext highlighter-rouge">or</code> keyword. Like and, it is a function that will trade you one bool for two bools.</p> <h4 id="logical-negation">logical negation</h4> <p>Occasionally, the <code class="language-plaintext highlighter-rouge">¬</code>, <code class="language-plaintext highlighter-rouge">~</code> and <code class="language-plaintext highlighter-rouge">!</code> symbols are used to represent logical <code class="language-plaintext highlighter-rouge">NOT</code>. For example, <em>¬A</em> is only true if A is false.</p> <p>Here is a simple example using the <em>not</em> symbol:</p> <p><img src="http://latex.codecogs.com/svg.latex?x%20%5Cneq%20y%20%5CLeftrightarrow%20%5Clnot%28x%20%3D%20y%29" alt="negation"/></p> <p>An example of how we might interpret this in code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">lambda</span> <span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">:</span> <span class="nf">if </span><span class="p">(</span><span class="n">x</span> <span class="o">!=</span> <span class="n">y</span><span class="p">):</span> <span class="k">assert</span> <span class="ow">not</span> <span class="n">x</span> <span class="o">==</span> <span class="n">y</span><span class="p">,</span> <span class="sh">"</span><span class="s">arrr, buried treasure lost forever. </span><span class="sh">"</span>
</code></pre></div></div> <p><em>Note:</em> The tilde <code class="language-plaintext highlighter-rouge">~</code> has many different meanings depending on context. For example, <em>row equivalence</em> (matrix theory) or <em>same order of magnitude</em> (discussed in <a href="#equality">equality</a>).</p> <h4 id="intervals">intervals</h4> <p>Sometimes a function deals with real numbers restricted to some range of values, such a constraint can be represented using an <em>interval</em></p> <p>For example we can represent the numbers between zero and one including/not including zero and/or one as:</p> <ul> <li>Not including zero or one: <img src="http://latex.codecogs.com/svg.latex?%280%2C%201%29" alt="interval-opened-left-opened-right"/></li> </ul> <ul> <li>Including zero or but not one: <img src="http://latex.codecogs.com/svg.latex?%5B0%2C%201%29" alt="interval-closed-left-opened-right"/></li> </ul> <ul> <li>Not including zero but including one: <img src="http://latex.codecogs.com/svg.latex?%280%2C%201%5D" alt="interval-opened-left-closed-right"/></li> </ul> <ul> <li>Including zero and one: <img src="http://latex.codecogs.com/svg.latex?%5B0%2C%201%5D" alt="interval-closed-left-closed-right"/></li> </ul> <p>For example we to indicate that a point <code class="language-plaintext highlighter-rouge">x</code> is in the unit cube in 3D we say:</p> <p><img src="http://latex.codecogs.com/svg.latex?x%20%5Cin%20%5B0%2C%201%5D%5E3" alt="interval-unit-cube"/></p> <p>In Python, we have to be sensitive about <strong>inclusive vs. exclusive boundaries</strong> in generators like <code class="language-plaintext highlighter-rouge">range</code>, but you already know that.</p> <p>if you want to play with <em>infinite lists</em> in Python, learn more about <a href="https://jeffknupp.com/blog/2013/04/07/improve-your-python-yield-and-generators-explained/">generators</a></p> <p>Intervals are used in conjunction with set operations:</p> <ul> <li><em>intersection</em> e.g. <img src="http://latex.codecogs.com/svg.latex?%5B3%2C%205%29%20%5Ccap%20%5B4%2C%206%5D%20%3D%20%5B4%2C%205%29" alt="interval-intersection"/></li> </ul> <ul> <li><em>union</em> e.g. <img src="http://latex.codecogs.com/svg.latex?%5B3%2C%205%29%20%5Ccup%20%5B4%2C%206%5D%20%3D%20%5B3%2C%206%5D" alt="interval-union"/></li> </ul> <ul> <li><em>difference</em> e.g. <img src="http://latex.codecogs.com/svg.latex?%5B3%2C%205%29%20-%20%5B4%2C%206%5D%20%3D%20%5B3%2C%204%29" alt="interval-difference-1"/> and <img src="http://latex.codecogs.com/svg.latex?%5B4%2C%206%5D%20-%20%5B3%2C%205%29%20%3D%20%5B5%2C%206%5D" alt="interval-difference-2"/></li> </ul> <p>Integer versions in basic python look like this</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># intersection of two int intervals
</span><span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span><span class="mi">5</span><span class="p">)</span> <span class="k">if</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">6</span><span class="o">+</span><span class="mi">1</span><span class="p">)]</span>
<span class="c1"># Out: [4]
</span>
<span class="c1"># Union of two int intervals
</span><span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">20</span><span class="p">)</span> <span class="k">if</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">)</span> <span class="ow">or</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">6</span><span class="o">+</span><span class="mi">1</span><span class="p">)]</span>
<span class="c1"># Out: [3, 4, 5, 6]
</span>
<span class="c1"># Set difference
</span><span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">)</span> <span class="k">if</span> <span class="n">x</span> <span class="ow">not</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">6</span><span class="o">+</span><span class="mi">1</span><span class="p">)]</span>
<span class="c1"># Out: [3]
</span>
<span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">6</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span> <span class="k">if</span> <span class="n">x</span> <span class="ow">not</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">)]</span>
<span class="c1"># Out: [5, 6]
</span></code></pre></div></div> <p>Using <code class="language-plaintext highlighter-rouge">np.linspace</code>, we can approximate what the real versions would look like.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">R</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">100</span><span class="p">)</span>

<span class="c1"># intersection of two float intervals
</span><span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">R</span> <span class="k">if</span> <span class="mi">3</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;</span> <span class="mi">5</span> <span class="ow">and</span> <span class="mi">4</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;=</span> <span class="mi">6</span><span class="p">]</span>

<span class="c1"># Union of two float intervals
</span><span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">R</span> <span class="k">if</span> <span class="mi">3</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;</span> <span class="mi">5</span> <span class="ow">or</span> <span class="mi">4</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;=</span> <span class="mi">6</span><span class="p">]</span>

<span class="c1"># set differences of two float intervals. 
</span><span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">R</span> <span class="k">if</span> <span class="mi">3</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;</span> <span class="mi">5</span> <span class="ow">and</span> <span class="ow">not</span> <span class="p">(</span><span class="mi">4</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;=</span> <span class="mi">6</span><span class="p">)]</span>

<span class="p">[</span><span class="n">x</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">R</span> <span class="k">if</span> <span class="mi">4</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;=</span> <span class="mi">6</span> <span class="ow">and</span> <span class="ow">not</span> <span class="p">(</span><span class="mi">3</span> <span class="o">&lt;=</span> <span class="n">x</span> <span class="o">&lt;</span> <span class="mi">5</span><span class="p">)]</span>
</code></pre></div></div> <p>You should definitely run these in repl and try to wrap your head around them.</p> <h3 id="license">License</h3> <p>MIT, see <a href="http://github.com/Jam3/math-as-code/blob/master/LICENSE.md">LICENSE.md</a> for details.</p>]]></content><author><name></name></author><category term="self-prepare"/><category term="basics"/><summary type="html"><![CDATA[Math notation basics.]]></summary></entry><entry><title type="html">W1 Course introduction</title><link href="/blog/2023/w1/" rel="alternate" type="text/html" title="W1 Course introduction"/><published>2023-07-24T00:00:00+00:00</published><updated>2023-07-24T00:00:00+00:00</updated><id>/blog/2023/w1</id><content type="html" xml:base="/blog/2023/w1/"><![CDATA[<hr/> <h1 id="key-points">Key points</h1> <hr/> <ul> <li>For all students: <ul> <li>Overview of empirical social science research (missing reference)</li> <li>Purposes of social science research (missing reference) <br/></li> </ul> </li> <li>For students who want to <a href="/test_quant/"><strong><em>Early-Pass</em></strong> the Standard Test - Quantitative</a>, the following readings are also required. <ul> <li>What is “reproducibility” and why it is important (missing reference)</li> <li>Typology of replication studies (missing reference)</li> </ul> </li> </ul> <p><br/></p> <hr/> <h1 id="before-class">Before class</h1> <hr/> <h2 id="required-readings">Required readings</h2> <h2 id="recommended-readings-for-early-pass">Recommended readings for <a href="/test_quant/">Early-Pass</a></h2> <ul> <li>(missing reference)</li> <li>(missing reference)</li> <li>(missing reference)</li> <li>(missing reference)</li> </ul> <h2 id="inspiring-video">Inspiring video</h2> <iframe width="560" height="315" src="https://www.youtube.com/embed/arj7oStGLkU" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen=""></iframe> <p><br/></p> <hr/> <h1 id="in-class">In class</h1> <hr/> <ul> <li>Course introduction: websites, Canvas, and other tools/platforms for this class (1h)</li> <li>Review Standard Tests (40 mins) <ul> <li><a href="https://amgps.jima.me/test_quant/">Quantitative</a></li> <li><a href="https://amgps.jima.me/test_qual/">Qualitative</a></li> </ul> </li> <li>Key concepts and frameworks (15 mins)</li> <li><a href="https://docs.google.com/document/d/1NzLq6amkq99BjbRk24eKve4v9EAES0JJLnSHM0498UI/edit?usp=sharing">Week 1 Q&amp;A</a> (30 mins)</li> </ul> <p><br/></p> <hr/> <h1 id="after-class">After class</h1> <hr/> <h2 id="prepare-articles-for-replications">Prepare articles for replications</h2> <p>Start to choose the article for the replication project. Prepare either ONE of the following items OR all the items for sharing next week. The general idea of this task is to push you to start–“Rome wasn’t built in a day.”</p> <ul> <li>An oral introduction about your replication interests, directions, and methods, etc.</li> <li>An oral introduction about the article(s) you may choose, why you choose them, and your replication plan.</li> <li>A short rough presentation about your replication plan.</li> </ul> <p><em>This after-class activity is NOT for grading, just for in-class sharing and discussion. This activity helps you prepare the replication plan, but it is NOT the replication plan. The articles you select are tentative. You can do it either individually or in a group. If you do this activity in a group, this activity helps you form a group, but the group may NOT be the final group for the replication project.</em></p> <h2 id="complete-the-pre-course-survey">Complete the <a href="https://utexas.instructure.com/courses/1338983/quizzes/1705746">Pre-course survey</a></h2>]]></content><author><name></name></author><category term="basics"/><summary type="html"><![CDATA[Review course schedule, assignments, and Q&A.]]></summary></entry><entry><title type="html">a post with bibliography</title><link href="/blog/2023/post-bibliography/" rel="alternate" type="text/html" title="a post with bibliography"/><published>2023-07-12T13:56:00+00:00</published><updated>2023-07-12T13:56:00+00:00</updated><id>/blog/2023/post-bibliography</id><content type="html" xml:base="/blog/2023/post-bibliography/"><![CDATA[<p>This post shows how to add bibliography to simple blog posts. If you would like something more academic, check the <a href="/blog/2021/distill/">distill style post</a>.</p>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="bib"/><summary type="html"><![CDATA[an example of a blog post with bibliography]]></summary></entry><entry><title type="html">a post with jupyter notebook</title><link href="/blog/2023/jupyter-notebook/" rel="alternate" type="text/html" title="a post with jupyter notebook"/><published>2023-07-04T12:57:00+00:00</published><updated>2023-07-04T12:57:00+00:00</updated><id>/blog/2023/jupyter-notebook</id><content type="html" xml:base="/blog/2023/jupyter-notebook/"><![CDATA[<p>To include a jupyter notebook in a post, you can use the following code:</p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{::nomarkdown}
{% assign jupyter_path = "assets/jupyter/blog.ipynb" | relative_url %}
{% capture notebook_exists %}{% file_exists assets/jupyter/blog.ipynb %}{% endcapture %}
{% if notebook_exists == "true" %}
    {% jupyter_notebook jupyter_path %}
{% else %}
    <span class="nt">&lt;p&gt;</span>Sorry, the notebook you are looking for does not exist.<span class="nt">&lt;/p&gt;</span>
{% endif %}
{:/nomarkdown}
</code></pre></div></div> <p>Let’s break it down: this is possible thanks to <a href="https://github.com/red-data-tools/jekyll-jupyter-notebook">Jekyll Jupyter Notebook plugin</a> that allows you to embed jupyter notebooks in your posts. It basically calls <a href="https://nbconvert.readthedocs.io/en/latest/usage.html#convert-html"><code class="language-plaintext highlighter-rouge">jupyter nbconvert --to html</code></a> to convert the notebook to an html page and then includes it in the post. Since <a href="https://jekyllrb.com/docs/configuration/markdown/">Kramdown</a> is the default Markdown renderer for Jekyll, we need to surround the call to the plugin with the <a href="https://kramdown.gettalong.org/syntax.html#extensions">::nomarkdown</a> tag so that it stops processing this part with Kramdown and outputs the content as-is.</p> <p>The plugin takes as input the path to the notebook, but it assumes the file exists. If you want to check if the file exists before calling the plugin, you can use the <code class="language-plaintext highlighter-rouge">file_exists</code> filter. This avoids getting a 404 error from the plugin and ending up displaying the main page inside of it instead. If the file does not exist, you can output a message to the user. The code displayed above outputs the following:</p> <div class="jupyter-notebook" style="position: relative; width: 100%; margin: 0 auto;"> <div class="jupyter-notebook-iframe-container"> <iframe src="/assets/jupyter/blog.ipynb.html" style="position: absolute; top: 0; left: 0; border-style: none;" width="100%" height="100%" onload="this.parentElement.style.paddingBottom = (this.contentWindow.document.documentElement.scrollHeight + 10) + 'px'"></iframe> </div> </div> <p>Note that the jupyter notebook supports both light and dark themes.</p>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="jupyter"/><summary type="html"><![CDATA[an example of a blog post with jupyter notebook]]></summary></entry><entry><title type="html">a post with custom blockquotes</title><link href="/blog/2023/custom-blockquotes/" rel="alternate" type="text/html" title="a post with custom blockquotes"/><published>2023-05-12T19:53:00+00:00</published><updated>2023-05-12T19:53:00+00:00</updated><id>/blog/2023/custom-blockquotes</id><content type="html" xml:base="/blog/2023/custom-blockquotes/"><![CDATA[<p>This post shows how to add custom styles for blockquotes. Based on <a href="https://github.com/sighingnow/jekyll-gitbook">jekyll-gitbook</a> implementation.</p> <p>We decided to support the same custom blockquotes as in <a href="https://sighingnow.github.io/jekyll-gitbook/jekyll/2022-06-30-tips_warnings_dangers.html">jekyll-gitbook</a>, which are also found in a lot of other sites’ styles. The styles definitions can be found on the <a href="https://github.com/alshedivat/al-folio/blob/master/_sass/_base.scss">_base.scss</a> file, more specifically:</p> <div class="language-scss highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* Tips, warnings, and dangers */</span>
<span class="nc">.post</span> <span class="nc">.post-content</span> <span class="nt">blockquote</span> <span class="p">{</span>
    <span class="k">&amp;</span><span class="nc">.block-tip</span> <span class="p">{</span>
    <span class="nl">border-color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-tip-block</span><span class="p">);</span>
    <span class="nl">background-color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-tip-block-bg</span><span class="p">);</span>

    <span class="nt">p</span> <span class="p">{</span>
      <span class="nl">color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-tip-block-text</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="nt">h1</span><span class="o">,</span> <span class="nt">h2</span><span class="o">,</span> <span class="nt">h3</span><span class="o">,</span> <span class="nt">h4</span><span class="o">,</span> <span class="nt">h5</span><span class="o">,</span> <span class="nt">h6</span> <span class="p">{</span>
      <span class="nl">color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-tip-block-title</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="k">&amp;</span><span class="nc">.block-warning</span> <span class="p">{</span>
    <span class="nl">border-color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-warning-block</span><span class="p">);</span>
    <span class="nl">background-color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-warning-block-bg</span><span class="p">);</span>

    <span class="nt">p</span> <span class="p">{</span>
      <span class="nl">color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-warning-block-text</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="nt">h1</span><span class="o">,</span> <span class="nt">h2</span><span class="o">,</span> <span class="nt">h3</span><span class="o">,</span> <span class="nt">h4</span><span class="o">,</span> <span class="nt">h5</span><span class="o">,</span> <span class="nt">h6</span> <span class="p">{</span>
      <span class="nl">color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-warning-block-title</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="k">&amp;</span><span class="nc">.block-danger</span> <span class="p">{</span>
    <span class="nl">border-color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-danger-block</span><span class="p">);</span>
    <span class="nl">background-color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-danger-block-bg</span><span class="p">);</span>

    <span class="nt">p</span> <span class="p">{</span>
      <span class="nl">color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-danger-block-text</span><span class="p">);</span>
    <span class="p">}</span>

    <span class="nt">h1</span><span class="o">,</span> <span class="nt">h2</span><span class="o">,</span> <span class="nt">h3</span><span class="o">,</span> <span class="nt">h4</span><span class="o">,</span> <span class="nt">h5</span><span class="o">,</span> <span class="nt">h6</span> <span class="p">{</span>
      <span class="nl">color</span><span class="p">:</span> <span class="nf">var</span><span class="p">(</span><span class="o">--</span><span class="n">global-danger-block-title</span><span class="p">);</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>A regular blockquote can be used as following:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gt">&gt; This is a regular blockquote</span>
<span class="gt">&gt; and it can be used as usual</span>
</code></pre></div></div> <blockquote> <p>This is a regular blockquote and it can be used as usual</p> </blockquote> <p>These custom styles can be used by adding the specific class to the blockquote, as follows:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gt">&gt; ##### TIP</span>
<span class="gt">&gt;</span>
<span class="gt">&gt; A tip can be used when you want to give advice</span>
<span class="gt">&gt; related to a certain content.</span>
{: .block-tip }
</code></pre></div></div> <blockquote class="block-tip"> <h5 id="tip">TIP</h5> <p>A tip can be used when you want to give advice related to a certain content.</p> </blockquote> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gt">&gt; ##### WARNING</span>
<span class="gt">&gt;</span>
<span class="gt">&gt; This is a warning, and thus should</span>
<span class="gt">&gt; be used when you want to warn the user</span>
{: .block-warning }
</code></pre></div></div> <blockquote class="block-warning"> <h5 id="warning">WARNING</h5> <p>This is a warning, and thus should be used when you want to warn the user</p> </blockquote> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gt">&gt; ##### DANGER</span>
<span class="gt">&gt;</span>
<span class="gt">&gt; This is a danger zone, and thus should</span>
<span class="gt">&gt; be used carefully</span>
{: .block-danger }
</code></pre></div></div> <blockquote class="block-danger"> <h5 id="danger">DANGER</h5> <p>This is a danger zone, and thus should be used carefully</p> </blockquote>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="blockquotes"/><summary type="html"><![CDATA[an example of a blog post with custom blockquotes]]></summary></entry><entry><title type="html">a post with table of contents on a sidebar</title><link href="/blog/2023/sidebar-table-of-contents/" rel="alternate" type="text/html" title="a post with table of contents on a sidebar"/><published>2023-04-25T14:14:00+00:00</published><updated>2023-04-25T14:14:00+00:00</updated><id>/blog/2023/sidebar-table-of-contents</id><content type="html" xml:base="/blog/2023/sidebar-table-of-contents/"><![CDATA[<p>This post shows how to add a table of contents as a sidebar.</p> <h2 id="adding-a-table-of-contents">Adding a Table of Contents</h2> <p>To add a table of contents to a post as a sidebar, simply add</p> <div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">toc</span><span class="pi">:</span>
  <span class="na">sidebar</span><span class="pi">:</span> <span class="s">left</span>
</code></pre></div></div> <p>to the front matter of the post. The table of contents will be automatically generated from the headings in the post. If you wish to display the sidebar to the right, simply change <code class="language-plaintext highlighter-rouge">left</code> to <code class="language-plaintext highlighter-rouge">right</code>.</p> <h3 id="example-of-sub-heading-1">Example of Sub-Heading 1</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p> <h3 id="example-of-another-sub-heading-1">Example of another Sub-Heading 1</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p> <h2 data-toc-text="Customizing" id="customizing-your-table-of-contents">Customizing Your Table of Contents</h2> <p>If you want to learn more about how to customize the table of contents of your sidebar, you can check the <a href="https://afeld.github.io/bootstrap-toc/">bootstrap-toc</a> documentation. Notice that you can even customize the text of the heading that will be displayed on the sidebar.</p> <h3 id="example-of-sub-heading-2">Example of Sub-Heading 2</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p> <h3 id="example-of-another-sub-heading-2">Example of another Sub-Heading 2</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="toc"/><category term="sidebar"/><summary type="html"><![CDATA[an example of a blog post with table of contents on a sidebar]]></summary></entry><entry><title type="html">a post with audios</title><link href="/blog/2023/audios/" rel="alternate" type="text/html" title="a post with audios"/><published>2023-04-25T10:25:00+00:00</published><updated>2023-04-25T10:25:00+00:00</updated><id>/blog/2023/audios</id><content type="html" xml:base="/blog/2023/audios/"><![CDATA[<p>This is an example post with audios. It supports local audio files.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <audio src="/assets/audio/epicaly-short-113909.mp3" controls=""/> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <audio src="https://cdn.pixabay.com/download/audio/2022/06/25/audio_69a61cd6d6.mp3" controls=""/> </figure> </div> </div> <div class="caption"> A simple, elegant caption looks good between video rows, after each row, or doesn't have to be there at all. </div>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="audios"/><summary type="html"><![CDATA[this is what included audios could look like]]></summary></entry><entry><title type="html">a post with videos</title><link href="/blog/2023/videos/" rel="alternate" type="text/html" title="a post with videos"/><published>2023-04-24T21:01:00+00:00</published><updated>2023-04-24T21:01:00+00:00</updated><id>/blog/2023/videos</id><content type="html" xml:base="/blog/2023/videos/"><![CDATA[<p>This is an example post with videos. It supports local video files.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/pexels-engin-akyurt-6069112-960x540-30fps.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" autoplay="" controls=""/> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <video src="/assets/video/pexels-engin-akyurt-6069112-960x540-30fps.mp4" class="img-fluid rounded z-depth-1" width="auto" height="auto" controls=""/> </figure> </div> </div> <div class="caption"> A simple, elegant caption looks good between video rows, after each row, or doesn't have to be there at all. </div> <p>It does also support embedding videos from different sources. Here are some examples:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <iframe src="https://www.youtube.com/embed/jNQXAC9IVRw" class="img-fluid rounded z-depth-1" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen="" width="auto" height="auto"/> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <iframe src="https://player.vimeo.com/video/524933864?h=1ac4fd9fb4&amp;title=0&amp;byline=0&amp;portrait=0" class="img-fluid rounded z-depth-1" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen="" width="auto" height="auto"/> </figure> </div> </div>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="videos"/><summary type="html"><![CDATA[this is what included videos could look like]]></summary></entry><entry><title type="html">displaying beautiful tables with Bootstrap Tables</title><link href="/blog/2023/tables/" rel="alternate" type="text/html" title="displaying beautiful tables with Bootstrap Tables"/><published>2023-03-20T18:37:00+00:00</published><updated>2023-03-20T18:37:00+00:00</updated><id>/blog/2023/tables</id><content type="html" xml:base="/blog/2023/tables/"><![CDATA[<p>Using markdown to display tables is easy. Just use the following syntax:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>| Left aligned | Center aligned | Right aligned |
| :----------- | :------------: | ------------: |
| Left 1       | center 1       | right 1       |
| Left 2       | center 2       | right 2       |
| Left 3       | center 3       | right 3       |
</code></pre></div></div> <p>That will generate:</p> <table> <thead> <tr> <th style="text-align: left">Left aligned</th> <th style="text-align: center">Center aligned</th> <th style="text-align: right">Right aligned</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Left 1</td> <td style="text-align: center">center 1</td> <td style="text-align: right">right 1</td> </tr> <tr> <td style="text-align: left">Left 2</td> <td style="text-align: center">center 2</td> <td style="text-align: right">right 2</td> </tr> <tr> <td style="text-align: left">Left 3</td> <td style="text-align: center">center 3</td> <td style="text-align: right">right 3</td> </tr> </tbody> </table> <p></p> <p>It is also possible to use HTML to display tables. For example, the following HTML code will display a table with <a href="https://bootstrap-table.com/">Bootstrap Table</a>, loaded from a JSON file:</p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;table</span>
  <span class="na">id=</span><span class="s">"table"</span>
  <span class="na">data-toggle=</span><span class="s">"table"</span>
  <span class="na">data-url=</span><span class="s">"{{ '/assets/json/table_data.json' | relative_url }}"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;thead&gt;</span>
    <span class="nt">&lt;tr&gt;</span>
      <span class="nt">&lt;th</span> <span class="na">data-field=</span><span class="s">"id"</span><span class="nt">&gt;</span>ID<span class="nt">&lt;/th&gt;</span>
      <span class="nt">&lt;th</span> <span class="na">data-field=</span><span class="s">"name"</span><span class="nt">&gt;</span>Item Name<span class="nt">&lt;/th&gt;</span>
      <span class="nt">&lt;th</span> <span class="na">data-field=</span><span class="s">"price"</span><span class="nt">&gt;</span>Item Price<span class="nt">&lt;/th&gt;</span>
    <span class="nt">&lt;/tr&gt;</span>
  <span class="nt">&lt;/thead&gt;</span>
<span class="nt">&lt;/table&gt;</span>
</code></pre></div></div> <table data-toggle="table" data-url="/assets/json/table_data.json"> <thead> <tr> <th data-field="id">ID</th> <th data-field="name">Item Name</th> <th data-field="price">Item Price</th> </tr> </thead> </table> <p></p> <p>By using <a href="https://bootstrap-table.com/">Bootstrap Table</a> it is possible to create pretty complex tables, with pagination, search, and more. For example, the following HTML code will display a table, loaded from a JSON file, with pagination, search, checkboxes, and header/content alignment. For more information, check the <a href="https://examples.bootstrap-table.com/index.html">documentation</a>.</p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;table</span>
  <span class="na">data-click-to-select=</span><span class="s">"true"</span>
  <span class="na">data-height=</span><span class="s">"460"</span>
  <span class="na">data-pagination=</span><span class="s">"true"</span>
  <span class="na">data-search=</span><span class="s">"true"</span>
  <span class="na">data-toggle=</span><span class="s">"table"</span>
  <span class="na">data-url=</span><span class="s">"{{ '/assets/json/table_data.json' | relative_url }}"</span><span class="nt">&gt;</span>
  <span class="nt">&lt;thead&gt;</span>
    <span class="nt">&lt;tr&gt;</span>
      <span class="nt">&lt;th</span> <span class="na">data-checkbox=</span><span class="s">"true"</span><span class="nt">&gt;&lt;/th&gt;</span>
      <span class="nt">&lt;th</span> <span class="na">data-field=</span><span class="s">"id"</span> <span class="na">data-halign=</span><span class="s">"left"</span> <span class="na">data-align=</span><span class="s">"center"</span> <span class="na">data-sortable=</span><span class="s">"true"</span><span class="nt">&gt;</span>ID<span class="nt">&lt;/th&gt;</span>
      <span class="nt">&lt;th</span> <span class="na">data-field=</span><span class="s">"name"</span> <span class="na">data-halign=</span><span class="s">"center"</span> <span class="na">data-align=</span><span class="s">"right"</span> <span class="na">data-sortable=</span><span class="s">"true"</span><span class="nt">&gt;</span>Item Name<span class="nt">&lt;/th&gt;</span>
      <span class="nt">&lt;th</span> <span class="na">data-field=</span><span class="s">"price"</span> <span class="na">data-halign=</span><span class="s">"right"</span> <span class="na">data-align=</span><span class="s">"left"</span> <span class="na">data-sortable=</span><span class="s">"true"</span><span class="nt">&gt;</span>Item Price<span class="nt">&lt;/th&gt;</span>
    <span class="nt">&lt;/tr&gt;</span>
  <span class="nt">&lt;/thead&gt;</span>
<span class="nt">&lt;/table&gt;</span>
</code></pre></div></div> <table data-click-to-select="true" data-height="460" data-pagination="true" data-search="true" data-toggle="table" data-url="/assets/json/table_data.json"> <thead> <tr> <th data-checkbox="true"></th> <th data-field="id" data-halign="left" data-align="center" data-sortable="true">ID</th> <th data-field="name" data-halign="center" data-align="right" data-sortable="true">Item Name</th> <th data-field="price" data-halign="right" data-align="left" data-sortable="true">Item Price</th> </tr> </thead> </table>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="tables"/><summary type="html"><![CDATA[an example of how to use Bootstrap Tables]]></summary></entry><entry><title type="html">a post with table of contents</title><link href="/blog/2023/table-of-contents/" rel="alternate" type="text/html" title="a post with table of contents"/><published>2023-03-20T15:59:00+00:00</published><updated>2023-03-20T15:59:00+00:00</updated><id>/blog/2023/table-of-contents</id><content type="html" xml:base="/blog/2023/table-of-contents/"><![CDATA[<p>This post shows how to add a table of contents in the beginning of the post.</p> <h2 id="adding-a-table-of-contents">Adding a Table of Contents</h2> <p>To add a table of contents to a post, simply add</p> <div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">toc</span><span class="pi">:</span>
  <span class="na">beginning</span><span class="pi">:</span> <span class="kc">true</span>
</code></pre></div></div> <p>to the front matter of the post. The table of contents will be automatically generated from the headings in the post.</p> <h3 id="example-of-sub-heading-1">Example of Sub-Heading 1</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p> <h3 id="example-of-another-sub-heading-1">Example of another Sub-Heading 1</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p> <h2 id="table-of-contents-options">Table of Contents Options</h2> <p>If you want to learn more about how to customize the table of contents, you can check the <a href="https://github.com/toshimaru/jekyll-toc">jekyll-toc</a> repository.</p> <h3 id="example-of-sub-heading-2">Example of Sub-Heading 2</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p> <h3 id="example-of-another-sub-heading-2">Example of another Sub-Heading 2</h3> <p>Jean shorts raw denim Vice normcore, art party High Life PBR skateboard stumptown vinyl kitsch. Four loko meh 8-bit, tousled banh mi tilde forage Schlitz dreamcatcher twee 3 wolf moon. Chambray asymmetrical paleo salvia, sartorial umami four loko master cleanse drinking vinegar brunch. <a href="https://www.pinterest.com">Pinterest</a> DIY authentic Schlitz, hoodie Intelligentsia butcher trust fund brunch shabby chic Kickstarter forage flexitarian. Direct trade <a href="https://en.wikipedia.org/wiki/Cold-pressed_juice">cold-pressed</a> meggings stumptown plaid, pop-up taxidermy. Hoodie XOXO fingerstache scenester Echo Park. Plaid ugh Wes Anderson, freegan pug selvage fanny pack leggings pickled food truck DIY irony Banksy.</p>]]></content><author><name></name></author><category term="sample-posts"/><category term="formatting"/><category term="toc"/><summary type="html"><![CDATA[an example of a blog post with table of contents]]></summary></entry></feed>